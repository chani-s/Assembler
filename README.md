# Assembler
A program that converts assembly code into machine language and displays errors and comments when required. The requested input is assembly files and the output is files containing the machine language code, converted to base 32.
